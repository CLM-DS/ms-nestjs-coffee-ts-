apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coffee-api.fullname" . }}-test-connection"
  labels:
    {{- include "coffee-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "coffee-api.fullname" . }}:{{ .Values.service.port }}/']
  restartPolicy: Never
